label: Slider
type: structure
fields:
  image: fields/image
  sizing:
    label: Größe
    type: select
    default: cover
    options:
      cover: Füllen (Cover)
      contain: Beinhalten (Contain)
