extends: pages/layout

title: Kontakt
image:
  icon: email

options:
  # access
  # changeSlug
  # changeStatus
  # changeTemplate
  # changeTitle
  # create
  # delete
  # duplicate
  # list
  # move
  # preview
  # sort
  # update

tabs:
  content:
    columns:
      main:
        fields:
          blocks: false

          subTitle:
            type: text
            label: Subtitle

          additionalLinks:
            type: structure
            label: Weitere Links
            fields:
              iconCategory:
                label: Font-Awesome Icon Kategorie
                type: text
                default: fa-solid
              iconName:
                label: Font-Awesome Icon Name
                type: text
              label:
                label: Bezeichner
                type: text
              link:
                Label: Link
                type: link
      sidebar: false
